import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary ────────────────────────────────────────────────────────────────
  static const Color primary     = Color(0xFF2563EB);
  static const Color primaryDark = Color(0xFF1D4ED8);
  static const Color primarySoft = Color(0xFFEFF6FF);
  static const Color primaryMid  = Color(0xFFBFDBFE);

  // ── Neutrals (Light) ───────────────────────────────────────────────────────
  static const Color bg          = Color(0xFFF8FAFC);
  static const Color white       = Color(0xFFFFFFFF);
  static const Color text        = Color(0xFF0F172A);
  static const Color textSec     = Color(0xFF475569);
  static const Color textTert    = Color(0xFF94A3B8);
  static const Color border      = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);

  // ── Semantic ───────────────────────────────────────────────────────────────
  static const Color success     = Color(0xFF16A34A);
  static const Color successSoft = Color(0xFFF0FDF4);
  static const Color amber       = Color(0xFFD97706);
  static const Color amberSoft   = Color(0xFFFFFBEB);
  static const Color rose        = Color(0xFFDC2626);
  static const Color roseSoft    = Color(0xFFFEF2F2);
  static const Color violet      = Color(0xFF7C3AED);
  static const Color violetSoft  = Color(0xFFF5F3FF);
  static const Color teal        = Color(0xFF0891B2);
  static const Color tealSoft    = Color(0xFFECFEFF);
  static const Color brown       = Color(0xFFB45309);
  static const Color brownSoft   = Color(0xFFFEF3C7);

  // Orange — used for 'internal' type
  static const Color orange         = Color(0xFFF97316);
  static const Color orangeSoft     = Color(0xFFFFEDD5);
  static const Color darkOrangeSoft = Color(0xFF7C2D12);

  // Indigo — used for 'attendance' type
  static const Color indigo         = Color(0xFF6366F1);
  static const Color indigoSoft     = Color(0xFFE0E7FF);

  // ── Dark Mode Neutrals ─────────────────────────────────────────────────────
  static const Color darkBg          = Color(0xFF0B1120);
  static const Color darkSurface     = Color(0xFF0F172A);
  static const Color darkCard        = Color(0xFF1E293B);
  static const Color darkBorder      = Color(0xFF1E293B);
  static const Color darkBorderLight = Color(0xFF334155);

  // ── Dark Mode Text ─────────────────────────────────────────────────────────
  static const Color darkText     = Color(0xFFF1F5F9);
  static const Color darkTextSec  = Color(0xFF94A3B8);
  static const Color darkTextTert = Color(0xFF475569);

  // ── Dark Mode Semantic Softs ───────────────────────────────────────────────
  static const Color darkPrimarySoft = Color(0xFF1E3A5F);
  static const Color darkSuccessSoft = Color(0xFF14532D);
  static const Color darkRoseSoft    = Color(0xFF4C1D1D);
  static const Color darkAmberSoft   = Color(0xFF451A03);
  static const Color darkVioletSoft  = Color(0xFF2E1065);
  static const Color darkTealSoft    = Color(0xFF164E63);
  static const Color darkIndigoSoft  = Color(0xFF1E1B4B);
  static const Color darkOrangeSoft2 = Color(0xFF7C2D12);
}
